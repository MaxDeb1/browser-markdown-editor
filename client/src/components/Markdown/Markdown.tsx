import "./Markdown.css"

const Markdown = () => {
    return (
        <div className="markdown">
            <div className="title">
                Markdown
            </div>
        </div>
    );
};

export default Markdown;